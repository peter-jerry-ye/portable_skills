interface:
  display_name: "Portable PII Wasm"
  short_description: "Offline PII scan and redaction"
  default_prompt: "Use $portable-pii-wasm to check these files for supported PII and create safe review output."

policy:
  allow_implicit_invocation: true
